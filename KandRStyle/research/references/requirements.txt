# Python dependencies for reference tracking and PDF download scripts
# Install with: python -m pip install -r requirements.txt
#
# IMPORTANT: These versions are compatible with Python 3.6.2+
# For Python 3.8+, consider upgrading to newer versions for better security and features.
# Python 3.6 reached end-of-life in December 2021 and no longer receives security updates.

# Core dependencies (Python 3.6-compatible versions)
openpyxl>=3.0.0,<3.1.0  # 3.1.0+ requires Python 3.7+
matplotlib>=3.0.0,<3.4.0  # 3.4.0+ requires Python 3.7+; 3.7.0+ requires Python 3.8+
numpy>=1.19.0,<1.20.0  # 1.20.0+ requires Python 3.7+; 1.24.0+ requires Python 3.8+
requests>=2.28.0,<2.32.0  # 2.28.x is the highest Python 3.6-compatible version with security fixes

# Playwright for PDF downloads (Python 3.6-compatible version)
# Note: If playwright installation fails on Windows, you may need to:
# 1. Upgrade pip first: python -m pip install --upgrade pip setuptools wheel
# 2. Then install playwright: python -m pip install playwright
# 3. Install browsers: python -m playwright install firefox
playwright>=1.11.0,<1.20.0  # 1.20.0+ requires Python 3.7+; 1.40.0+ requires Python 3.8+
